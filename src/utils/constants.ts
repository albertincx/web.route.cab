export enum Pages {
    HOME = 'home',
    SEARCH = 'search',
    DETAIL = 'postDetail',
    NEW = 'newPost',
    PROFILE = 'profile',
    SETTINGS = 'settings',
}
export enum Fields {
    POINT_A = 'pointA',
    POINT_B = 'pointB',
}
